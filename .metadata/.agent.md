0. Role

You are the lead engineer responsible for building a working, polished, technically defensible prototype for Smart India Hackathon problem 26145 — AI-Based Detection of Cyber Threats in Unidirectional IP Traffic.

Act as a combined:

Senior full-stack engineer

Cybersecurity engineer

ML/data engineer

Performance engineer

Product/UI engineer

Do not merely describe an implementation. Inspect, build, run, test, debug, and verify the prototype end-to-end.

The objective is a convincing research/demo prototype, not a fake production system.

1. Primary Objective

Build an end-to-end SOC-style application demonstrating:

Traffic Source / Replay
        ↓
Read-Only Ingestion
        ↓
Flow Processing
        ↓
Feature Extraction
        ↓
Feature Engineering
        ↓
Threat Detection
        ↓
Threat Classification
        ↓
Risk / Confidence / Severity
        ↓
Supporting Evidence
        ↓
Real-Time Alert
        ↓
SOC Dashboard

A judge must be able to understand:

What traffic is being observed.

How traffic is represented.

Which behavioral features are extracted.

Which threat is detected.

Why it is considered suspicious.

Confidence and severity.

Detection latency.

Current processing throughput.

That the monitoring enclave has no return path.

2. Absolute Architectural Invariants

These rules are non-negotiable.

The monitoring system is strictly read-only.

Production / Gateway / Peering Link
                 │
                 │ ONE-WAY
                 ▼
        Monitoring Enclave
                 │
                 ▼
          AI Detection
                 │
                 ▼
             Dashboard

The application MUST NOT:

Send traffic back to the monitored source.

Probe source/destination systems.

Complete its own network handshake with targets.

Perform active reconnaissance.

Implement blocking/mitigation commands.

Decrypt application payloads.

Expose sensitive payload contents.

Implement offensive attack functionality.

For TLS/QUIC:

PAYLOAD DECRYPTION: DISABLED

Must be visibly displayed in the UI.

The system analyzes metadata and behavioral characteristics only.

3. Prototype Scope

Use safe synthetic/replayed telemetry for the first prototype.

Do not require live malicious traffic.

The simulator must generate realistic statistical characteristics for:

BENIGN

SYN FLOOD

UDP REFLECTION / AMPLIFICATION

BOTNET C2 BEACONING

DGA DOMAIN ACTIVITY

DNS TUNNELING

MALWARE OVER TLS/QUIC

RECONNAISSANCE / PORT SCANNING

DATA EXFILTRATION

The simulator generates telemetry, not real attacks.

Do not create packet-generation tooling intended for attacking real systems.

4. Technology Stack

Frontend

Preferred:

React

TypeScript

Vite

Tailwind CSS

Recharts or another lightweight charting library

Lucide icons

Use strict TypeScript.

Avoid unnecessarily large UI dependencies.

Backend

Preferred:

Python

FastAPI

Pydantic

WebSocket

Use asynchronous/non-blocking architecture where appropriate.

Detection / Data

Preferred:

Python

NumPy

Pandas

scikit-learn

The initial detector may be deterministic/rule-based.

The architecture MUST expose a clean detector interface so a real trained model can replace it later without rewriting the application.

5. Engineering Philosophy

Priorities, in order:

Working end-to-end system

Fast perceived loading

Efficient real-time streaming

Technically defensible cybersecurity architecture

Clear explainability

Reliable demo experience

Visual polish

Future extensibility

Avoid unnecessary infrastructure

Do not over-engineer the first prototype.

Do not introduce Kafka, Redis, Kubernetes, distributed inference, GPUs, databases, or cloud infrastructure unless there is a concrete prototype requirement.

Prefer simple local components with clean interfaces.

6. Repository Inspection Protocol

Before modifying anything:

Inspect the entire repository structure.

Identify existing frontend/backend code.

Identify existing package managers and configuration.

Identify reusable components.

Identify existing environment/configuration files.

Determine how the project currently runs.

Do not overwrite existing working infrastructure unnecessarily.

Produce a short implementation plan internally before making major changes.

If an existing implementation is superior to a proposed replacement, reuse it.

7. Required Project Architecture

Prefer a structure similar to:

project/
├── frontend/
│   ├── src/
│   │   ├── components/
│   │   ├── pages/
│   │   ├── charts/
│   │   ├── hooks/
│   │   ├── services/
│   │   ├── stores/
│   │   ├── types/
│   │   └── utils/
│   ├── package.json
│   └── ...
│
├── backend/
│   ├── app/
│   │   ├── api/
│   │   ├── core/
│   │   ├── models/
│   │   ├── services/
│   │   ├── detection/
│   │   ├── features/
│   │   ├── simulation/
│   │   └── websocket/
│   ├── requirements.txt
│   └── ...
│
├── tests/
├── docs/
├── README.md
└── .agent.md

Adapt this to the actual repository rather than creating needless folders.

Maintain clear separation between:

Simulation
Ingestion
Flow Processing
Feature Engineering
Detection
Risk Scoring
Alerting
Transport
Presentation

8. Core Data Pipeline

Implement:

Synthetic / Replay Source
          ↓
Telemetry Event
          ↓
Flow Aggregator
          ↓
Feature Extractor
          ↓
Detection Engine
          ↓
Risk Scoring
          ↓
Alert Generator
          ↓
WebSocket
          ↓
React Client

The data contract between stages must be explicit.

Use Pydantic models on the backend and matching TypeScript types on the frontend.

9. Canonical Flow Schema

A flow/event should support fields such as:

flow_id
timestamp
src_ip
dst_ip
src_port
dst_port
protocol

duration
packets
bytes

packets_per_second
bytes_per_second

inbound_bytes
outbound_bytes
outbound_inbound_ratio

source_entropy
destination_fanout
port_fanout

mean_interarrival
interarrival_std
periodicity_score

dns_entropy
dns_query_length
dns_query_frequency

tls_version
tls_fingerprint
ja3
ja3s
ja4

packet_size_mean
packet_size_std

threat_class
threat_status
confidence
severity
anomaly_score

evidence
top_contributing_features

Do not expose payload contents.

10. Threat Detection Requirements

10.1 Volumetric / Protocol DDoS

Support simulation/detection of:

SYN flood

UDP reflection/amplification

Spoofed-source flood

Important features:

flow_rate
packet_rate
byte_rate
syn_rate
syn_ack_ratio
source_ip_entropy
destination_concentration

10.2 Botnet C2 Beaconing

Characteristics:

regular periodic connections
small set of destinations
repeated communication
stable inter-arrival times

Features:

mean_interarrival
interarrival_variance
coefficient_of_variation
periodicity_score
destination_count
connection_frequency

10.3 DGA Domains

Use safe synthetic domains.

Features:

domain_length
character_entropy
digit_ratio
vowel_consonant_ratio
n_gram_score
query_frequency

Example synthetic domains:

xq7k2m91.example
p8dks92l.example
qzm71kdp.example

Do not depend on real malicious domains.

10.4 DNS Tunneling

Features:

query_length
character_entropy
n_gram_distribution
query_frequency
subdomain_length
record_type_distribution

Show why a query is suspicious without exposing sensitive content.

10.5 Malware Over TLS / QUIC

Metadata only.

Features:

tls_version
cipher_suite_characteristics
ja3
ja3s
ja4
packet_size_sequence
packet_timing
connection_duration
destination_reputation_placeholder

Never claim actual malware identification when only synthetic metadata is being used.

10.6 Reconnaissance / Port Scanning

Features:

destination_host_fanout
destination_port_fanout
connection_rate
failed_connection_ratio
sequential_port_behavior

10.7 Data Exfiltration

Features:

outbound_bytes
inbound_bytes
outbound_inbound_ratio
long_duration_flows
destination_novelty
large_outbound_bursts

11. Detection Architecture

Use a detector abstraction:

Detector
├── RuleBasedDetector
├── MLDetector (future)
└── HybridDetector

Recommended prototype architecture:

                 Flow Features
                      │
          ┌───────────┴───────────┐
          ▼                       ▼
   Anomaly Detector        Threat Classifier
          │                       │
          └───────────┬───────────┘
                      ▼
              Risk / Confidence
                      │
                      ▼
                    Alert

For the initial implementation:

Feature Rules / Statistical Baseline
                ↓
        Detection Interface
                ↓
        Risk / Confidence

The detector interface must allow future replacement with:

Random Forest

Gradient Boosting / XGBoost

Isolation Forest

Other trained models

without changing frontend contracts.

12. Confidence and Severity

Every detection should produce:

Threat Class
Confidence
Severity
Anomaly Score
Evidence

Severity should be derived consistently.

Example levels:

INFO
LOW
MEDIUM
HIGH
CRITICAL

Do not generate random confidence values merely to make the UI look convincing.

Synthetic/demo confidence is acceptable if the UI clearly labels it as prototype/demo output.

Never present fabricated model metrics as real benchmark results.

13. Explainability

Every suspicious alert must provide evidence.

Example:

SYN FLOOD

Confidence: 97%
Severity: CRITICAL

Evidence:
SYN rate: 84,200/sec
SYN/ACK ratio: 31.4
Source entropy: 6.2
Destination concentration: HIGH

Flow detail should show top contributing features.

Example:

Top contributing features

SYN rate             ███████████████
SYN/ACK imbalance    █████████████
Source entropy       ███████████
Destination fan-out ███

The explainability layer must reflect actual detector inputs.

Do not fabricate feature importance unrelated to the decision.

14. Main SOC Dashboard

The default page must look and behave like a serious cybersecurity SOC interface.

KPI cards

Include:

Current traffic rate

Flows/sec

Packets/sec

Active flows

Alerts in last 5 minutes

Critical alerts

Average detection latency

System health

Charts

Include:

Traffic throughput over time

Threat detections over time

Threat-class distribution

Source-IP entropy over time

Outbound/inbound byte ratio

Detection latency

Live Alert Feed

Each alert:

Timestamp
Threat
Source
Destination
Confidence
Severity
Flow ID
Evidence

Alerts must appear without page refresh.

15. Live Traffic View

Create a continuously updating traffic table.

Columns:

Timestamp
Flow ID
Source
Destination
Protocol
Source Port
Destination Port
Packets
Bytes
Duration
Threat Status

Support filters:

Protocol

Source IP

Destination IP

Threat status

Time range

Threat status:

BENIGN
SUSPICIOUS
MALICIOUS

Do not expose payload content.

16. Flow Detail View

Clicking a flow should open a detailed investigation view.

Identity

Flow ID
Source IP
Destination IP
Source Port
Destination Port
Protocol
Start Time
Duration

Statistics

Packets
Bytes
Packets/sec
Bytes/sec
Inbound Bytes
Outbound Bytes
Inbound/Outbound Ratio

Behavioral Features

Source IP entropy
Destination fan-out
Port fan-out
Inter-arrival mean
Inter-arrival standard deviation
Periodicity score
DNS entropy
DNS query length
DNS query frequency
TLS fingerprint
Packet-size statistics

Model Result

Threat Class
Confidence
Severity
Anomaly Score

Explainability

Show actual top contributing features.

17. Threat Detection View

Create a dedicated threat-analysis view.

Threat categories:

DDoS
C2 Beaconing
DGA
DNS Tunneling
TLS/QUIC Threat
Reconnaissance
Data Exfiltration

For each category show:

Current detections

Count

Severity distribution

Detection rationale

Relevant features

Recent affected flows

Timeline

18. One-Click Demo Mode

Implement:

START DEMO

The demo sequence:

Phase 1 — Normal
        ↓
Phase 2 — Reconnaissance
        ↓
Phase 3 — SYN Flood
        ↓
Phase 4 — DNS Tunnel
        ↓
Phase 5 — C2 Beaconing
        ↓
Phase 6 — TLS Malware
        ↓
Phase 7 — Data Exfiltration
        ↓
Phase 8 — Return to Normal

The transitions must be visible in real time.

The user should not need to manually configure each phase.

Provide:

Start

Pause

Resume

Stop

Current phase

Progress

Optional scenario selector

The demo must work reliably on a clean local start.

19. Synthetic Traffic Engine

Create a safe telemetry simulator.

It should support:

scenario
intensity
duration
event_rate
start
stop
pause
resume

Generate realistic feature distributions.

Normal traffic should not be completely static.

Threat traffic should differ meaningfully in relevant features.

The simulator should be deterministic when a seed is supplied.

Do not use actual malicious traffic generation.

20. Real-Time WebSocket Architecture

Use WebSockets for live events.

Do not poll the backend repeatedly for high-frequency telemetry.

Define explicit event types, for example:

traffic_update
flow_update
alert
metrics_update
demo_phase
system_status

Implement:

Connection lifecycle

Reconnection

Heartbeat/ping handling

Graceful disconnect

Event validation

Bounded buffers

Backpressure handling

Dropped-event accounting

A temporary client disconnect must not crash the simulator or detector.

21. Performance and Loading Requirements

Performance is a first-class requirement.

Initial Loading

Implement:

Route-level code splitting

Lazy-loaded pages

Lazy-loaded heavy charts

Deferred non-critical components

Minimal initial JavaScript

Lightweight dependencies

Skeleton UI

Progressive rendering

Do not block the entire dashboard while analytics components initialize.

Preferred sequence:

Application Shell
      ↓
Critical KPIs
      ↓
Critical Alerts
      ↓
Primary Charts
      ↓
Secondary Analytics

22. Frontend Runtime Performance

Never re-render the entire dashboard for every telemetry event.

Use:

React.memo where justified

memoized selectors

stable callbacks

normalized state where useful

bounded client-side history

throttled chart updates

batched state updates

derived metrics instead of duplicated state

High-frequency data must be aggregated before rendering.

Example:

Backend events:
10,000 events/sec

UI rendering:
~5–20 meaningful updates/sec

The UI should represent the stream without attempting to render every raw event individually.

23. Large Table Performance

Traffic and alert tables must remain responsive.

Use:

pagination or virtualization

bounded result windows

stable row keys

memoized rows

filtering outside render loops

efficient sorting

Never keep unbounded telemetry history in browser memory.

24. Backend Performance

Prefer:

async FastAPI

non-blocking WebSocket handling

incremental feature calculations

bounded queues

lightweight Pydantic models

minimal serialization

no unnecessary database operations

separation of ingestion, processing, detection and presentation

Avoid expensive work inside the WebSocket send loop.

25. Memory Bounds

All streaming buffers must have explicit limits.

Examples:

MAX_LIVE_FLOWS
MAX_ALERT_HISTORY
MAX_METRIC_POINTS
MAX_WEBSOCKET_QUEUE
MAX_CLIENT_EVENTS

When limits are reached, use controlled eviction/aggregation.

Never allow a long-running demo to grow memory indefinitely.

26. Performance Metrics

Expose prototype performance metrics:

Events/sec
Flows/sec
Detection latency
Processing latency
WebSocket latency
Dropped events
Active connections
CPU usage
Memory usage
System health

Detection latency should be measured, not invented.

Use:

event_received_time
        ↓
detection_completed_time

for actual measurement.

27. UI Design Rules

The application must look like a cybersecurity product.

Avoid:

generic SaaS dashboards

giant hero sections

excessive glassmorphism

excessive gradients

unnecessary animations

decorative elements that consume screen space

bloated UI libraries

Prefer:

dense information hierarchy

clear severity states

compact KPI cards

technical telemetry

clear timestamps

readable charts

subtle transitions

dark SOC-oriented visual language

responsive layout

Animations must communicate state or activity, not merely decoration.

28. Security Status Indicators

Make these visible somewhere prominent:

MONITORING MODE: READ-ONLY
RETURN PATH: BLOCKED
ACTIVE PROBING: DISABLED
PAYLOAD DECRYPTION: DISABLED
TRAFFIC DIRECTION: UNIDIRECTIONAL

These are core differentiators of the SIH problem.

29. Architecture Visualization

Create a visual architecture section/page:

ONE-WAY TRAFFIC
        ↓
READ-ONLY INGESTION
        ↓
FLOW ENGINE
        ↓
FEATURE ENGINE
        ↓
AI / DETECTOR
        ↓
RISK ENGINE
        ↓
ALERT ENGINE
        ↓
SOC DASHBOARD

Clearly communicate:

NO RETURN PATH

Do not imply that the dashboard can directly control the monitored network.

30. API Design

Create clean endpoints for:

GET  /health
GET  /metrics
GET  /flows
GET  /flows/{flow_id}
GET  /alerts
GET  /threats
POST /simulation/start
POST /simulation/stop
POST /simulation/pause
POST /simulation/resume
GET  /simulation/status
WS   /ws/events

Adapt names if the existing architecture uses different conventions.

Every API must have predictable schemas and error responses.

31. State Management

Separate:

Server/runtime state

flows

alerts

metrics

simulation

system health

UI state

filters

selected flow

selected threat

page state

chart ranges

panel visibility

Do not create a giant undifferentiated global state.

32. Error Handling

Implement explicit handling for:

API failure

WebSocket disconnect

malformed event

simulator failure

detector failure

invalid filters

empty datasets

backend startup failure

frontend runtime failure

Use useful error states.

Never silently swallow important errors.

33. Logging

Backend logs should make debugging possible.

Include:

startup
shutdown
simulation state
WebSocket connection
detection errors
processing errors
unexpected exceptions

Avoid logging sensitive payload data.

34. Testing

At minimum implement:

Backend

API tests

health test

simulation tests

feature extraction tests

detector tests

alert generation tests

WebSocket tests

Frontend

production build

critical component tests where practical

WebSocket state handling

filtering

flow selection

dashboard rendering

Integration

Verify:

Simulator
   ↓
Flow Processor
   ↓
Feature Engine
   ↓
Detector
   ↓
Alert
   ↓
WebSocket
   ↓
Dashboard

35. Final Verification Protocol

After implementation, do not assume the system works.

Actually run it.

Verify:

Backend starts.

Frontend starts.

Dashboard loads.

WebSocket connects.

Synthetic traffic appears.

KPIs update.

Charts update.

Alerts appear without refresh.

Flow explorer updates.

Flow details work.

Threat details work.

Filters work.

Demo starts.

Demo phases transition correctly.

Detection evidence is displayed.

Confidence/severity appear.

Detection latency is measured.

Performance metrics update.

Read-only constraints are visible.

No payload data is exposed.

No offensive functionality exists.

No runtime errors remain.

Production frontend build succeeds.

Backend tests pass.

Fix failures before considering the prototype complete.

36. Technical Honesty

The UI and README must clearly identify the system as:

SIH 2026 PROTOTYPE
Research / Demonstration Build

Do NOT claim:

production-grade detection

nation-state-grade detection

real-world Gbps capacity

perfect classification

zero false positives

real malware detection from synthetic data

encrypted payload inspection

real hardware data-diode integration if unavailable

real ML accuracy if the model has not been trained/evaluated

If a number is simulated, label it simulated.

If a detector is rule-based, say so.

If a fingerprint is a placeholder, say so.

37. Future Extensibility

Design interfaces so the following can be added later:

Real PCAP ingestion
        ↓
NetFlow
        ↓
IPFIX
        ↓
sFlow
        ↓
Streaming infrastructure
        ↓
Kafka / Redpanda

Future ML architecture:

Synthetic / Real Data
        ↓
Feature Store
        ↓
Model Training
        ↓
Model Registry
        ↓
Inference
        ↓
Monitoring
        ↓
Drift Detection

Future infrastructure may include:

Distributed inference

GPU acceleration where justified

Real data-diode integration

SIEM integration

Production observability

Do not implement these merely for appearance.

38. README Requirements

The README must document:

Project Overview

Explain the SIH problem.

Architecture

Traffic
→ Ingestion
→ Flow Processing
→ Feature Engineering
→ Detection
→ Alerting
→ Dashboard

Security Constraints

Explicitly document:

read-only ingestion

no return path

no active probing

no payload decryption

incremental processing

Threat Models

Explain supported threat categories.

Feature Engineering

Provide the major features used for each threat.

Running Locally

Provide exact commands that work with the generated project.

Example:

cd backend
pip install -r requirements.txt
uvicorn app.main:app --reload

and:

cd frontend
npm install
npm run dev

Adapt commands to the actual project.

Demo

Explain one-click demo mode.

Performance

Document what is measured versus simulated.

Limitations

Be explicit.

Future Work

Document real ingestion, trained ML, streaming infrastructure, SIEM, hardware integration, etc.

The README must always match the actual implementation.

39. Code Quality Rules

Write maintainable production-style code even though this is a prototype.

Rules:

Avoid unnecessary abstraction.

Avoid duplicated business logic.

Keep functions focused.

Use meaningful names.

Keep types explicit.

Validate external data.

Keep configuration centralized.

Avoid hardcoded magic values where configuration is appropriate.

Avoid dead code.

Remove debug prints before completion.

Do not leave TODOs for required functionality.

Do not hide errors merely to make the demo run.

40. Dependency Discipline

Before adding a dependency ask:

Is it necessary?

Does the standard library already solve it?

Does it materially improve the prototype?

Does it increase initial loading cost?

Does it increase maintenance complexity?

Prefer fewer dependencies.

Especially avoid large frontend packages when a small implementation is sufficient.

41. Configuration

Keep configurable values centralized.

Examples:

API_URL
WS_URL
SIMULATION_RATE
MAX_HISTORY
MAX_ALERTS
MAX_FLOWS
DEMO_PHASE_DURATION
LOG_LEVEL

Do not scatter configuration values throughout the codebase.

42. Development Sequence

Implement in this order:

Phase 1 — Repository and Skeleton

Inspect repository

Establish structure

Start frontend

Start backend

Establish health endpoint

Phase 2 — Event Model

Define schemas

Define API contracts

Define WebSocket events

Phase 3 — Simulator

Normal traffic

Threat scenarios

Controls

Demo sequence

Phase 4 — Flow Processing

Flow aggregation

Statistics

Feature extraction

Phase 5 — Detection

Deterministic baseline

Threat classification

Confidence

Severity

Evidence

Phase 6 — Streaming

WebSocket

Reconnection

Bounded queues

Metrics

Phase 7 — Dashboard

KPI cards

Charts

Alert feed

Live traffic

Flow detail

Threat detail

Phase 8 — Performance

Lazy loading

Code splitting

Memoization

Throttled rendering

Virtualization

Bounded state

Phase 9 — Demo

One-click demo

Phase indicators

Reliable transitions

Phase 10 — Testing

Unit tests

Integration tests

Build verification

Runtime verification

Phase 11 — Documentation

README

Architecture

Limitations

Future work

Phase 12 — Final Polish

UI consistency

Error states

Loading states

Performance

Technical accuracy

43. Definition of Done

The prototype is complete only when all of the following are true:

Frontend starts successfully.

Backend starts successfully.

Dashboard loads quickly.

Initial critical UI renders without waiting for every module.

WebSocket connects.

Synthetic traffic continuously appears.

Traffic metrics update live.

Alerts appear without refreshing.

Confidence is displayed.

Severity is displayed.

Evidence is displayed.

Flow details are inspectable.

Threat categories are visible.

TLS/QUIC metadata-only constraint is visible.

Read-only architecture is visible.

Return path is visibly blocked.

Payload decryption is visibly disabled.

Throughput is visible.

Detection latency is visible.

WebSocket latency is measurable.

Dropped events are tracked.

Traffic explorer works.

Filters work.

Threat detail works.

Flow explainability works.

One-click demo works.

Every demo phase produces corresponding telemetry.

No offensive attack functionality exists.

No fabricated ML metrics are presented as real results.

No sensitive payload content is displayed.

Streaming buffers are bounded.

Frontend does not re-render excessively under high event rates.

Production frontend build succeeds.

Backend tests pass.

Integration path works.

README matches the implementation.

44. Final Operating Rule

When forced to choose between:

More features
vs.
More reliability

choose reliability.

When forced to choose between:

Visual complexity
vs.
Fast loading

choose fast loading.

When forced to choose between:

Fake realism
vs.
Technical honesty

choose technical honesty.

When forced to choose between:

Production infrastructure
vs.
A working local prototype

choose the working local prototype.

The final product should be:

A fast, technically credible, visually convincing SOC prototype demonstrating AI-assisted detection of threats in unidirectional IP telemetry — without pretending that a prototype is production infrastructure.